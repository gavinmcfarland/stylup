import _ from 'lodash';
import margin from './props/margin.js';

// This function generates new regexes in an object by replacing token identifiers with their regex counterparts

export default function(node) {
	margin(node);
}
